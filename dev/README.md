# Dev Wrappers

This folder contains developer-only Windows batch wrappers for:

- base distro builder/export/import testing
- light finalize testing
- fresh install checking
- test-branch installer launch

They were moved out of the repo root so the normal user-facing entry points stay cleaner.

The older PowerShell-first installer bootstrap now lives under:

- `legacy/`
