# Legacy Installer Bootstrap

These files are archived from the older PowerShell-first installer flow.

They are not the main install entrypoint anymore.

Use the Windows installer app instead:

- `apps/Nymphs3DInstaller`

Archived files here:

- `Install_Nymphs3D.bat`
- `Install_Nymphs3D.ps1`
- `DOUBLE_CLICK_THIS_TO_TEST_INSTALLER.bat`

Archived docs for that older flow now live under:

- `docs/archive/legacy-installer/`
